# Copy to apps/mobile/.env — NEVER commit the real file.
#
# The anon key is SAFE to ship: it goes inside the app binary, and every rule
# (disclosure ladder, 25-mile cap, block, 18+) is enforced by RLS behind it.
# That is exactly why those rules live in Postgres and not in app code.
EXPO_PUBLIC_SUPABASE_URL=https://lyrynfmmobtvpydmisbf.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# NEVER put the service_role key or the database password in any file in this
# repo. service_role bypasses every policy. The DB password is superuser access.
